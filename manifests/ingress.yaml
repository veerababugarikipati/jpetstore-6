apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: jpetstore-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: aks3939.20.81.78.25.nip.io
    http:
      paths:
      - backend:
          serviceName: jpetstore
          servicePort: 80
        path: /jpetstore
